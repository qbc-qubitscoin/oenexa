server:
  port: 8088
spring:
  application:
    name: oenexa-security-service
  datasource:
    url: jdbc:mysql://localhost:3306/security_db?createDatabaseIfNotExist=true
    username: root
    password: password
  kafka:
    bootstrap-servers: localhost:9092
  elasticsearch:
    uris: http://localhost:9200
