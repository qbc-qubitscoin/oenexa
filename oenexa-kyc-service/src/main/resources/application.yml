server:
  port: 8083

spring:
  application:
    name: oenexa-kyc-service
  datasource:
    url: jdbc:mysql://localhost:3306/kyc_db
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
